- Проверять слайс на пустоту с помощью len(list) == nil
- По возможности аллоцировать память для слайса
- Если хотим изменить переданный слайс, то создаём его копию
- Результат append присваиваем исходному слайсу(той же самой переменной)